apiVersion: apps/v1
kind: Deployment
metadata:
  name: ocean-frontend-deploy
spec:
  selector:
    matchLabels:
      app: ocean-frontend-deploy
  replicas: {{ .Values.frontend.replicas }}
  template:
    metadata:
      labels:
        app: ocean-frontend-deploy
    spec:
      containers:
        - name: ocean-frontend-con
          image: {{ .Values.frontend.image.name }}:{{ .Values.frontend.image.tag }}
          command: ["/bin/sh", "-c"]
          args:
            - "envsubst '${OCEAN_BACKEND}' < /etc/nginx/nginx.conf.template > /etc/nginx/nginx.conf && nginx -g 'daemon off;'"
          imagePullPolicy: {{ .Values.frontend.image.imagePullPolicy }}
          ports:
            - containerPort: 80
              name: frontend-port
          env:
          - name: OCEAN_BACKEND
            value: ocean-backend-svc:8000
          resources:
            limits:
              cpu: {{ .Values.frontend.resources.limits.cpu }}
              memory: {{ .Values.frontend.resources.limits.memory }}
            requests:
              cpu: {{ .Values.frontend.resources.requests.cpu }}
              memory: {{ .Values.frontend.resources.requests.memory }}
      imagePullSecrets:
        - name: {{ .Values.frontend.image.imagePullSecret }}
